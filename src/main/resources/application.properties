spring.application.name=Knotly_WeddingInvitation

spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
#
#spring.datasource.url=${DB_URL}
#spring.datasource.username=${DB_USER}
#spring.datasource.password=${DB_PASSWORD}
spring.datasource.url=jdbc:sqlserver://DESKTOP-UQQT8K4\\YUKIBEVIP;databaseName=Knotly;encrypt=false;trustServerCertificate=true;loginTimeout=30;
spring.datasource.username=sa
spring.datasource.password=123
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect

spring.security.oauth2.client.registration.google.client-id=${GG_ID}
spring.security.oauth2.client.registration.google.client-secret=${GG_SECRET}
spring.security.oauth2.client.registration.google.scope=email,profile

# Core PayOS credentials
payos.client-id=${PAY_ID}
payos.api-key=${PAY_API_KEY}
payos.checksum-key=${PAY_CHECKSUM_KEY}

# Optional: Payout credentials (leave empty if not using payouts)
payos.payout-client-id=
payos.payout-api-key=
payos.payout-checksum-key=

# Logging level: DEBUG | INFO | NONE
payos.log-level=NONE

# Port configuration - reads from environment variable PORT (Azure sets this)
server.port=${PORT:${HTTP_PLATFORM_PORT:8080}}
